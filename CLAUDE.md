# CLAUDE.md — دستورالعمل عامل (Claude Code) برای این پروژه

> این فایل «قانون اساسی» کار توئه. قبل از هر اقدامی این فایل رو کامل بخون. اگر جایی از `docs/PRD.md`، `docs/ARCHITECTURE.md` یا `docs/ROADMAP.md` مغایرت داشت با درخواست لحظه‌ای کاربر، همیشه از کاربر تأیید بگیر، حدس نزن.

## ۱. پروژه چیست؟

ساخت یک **پلتفرم شبکه تبلیغاتی تلگرام** مشابه ربات‌هایی مثل "Wise | Telegram Ads":
- تبلیغ‌دهنده‌ها تبلیغ می‌سازن و در کانال‌ها/ربات‌ها/نتایج جستجوی تلگرام هدف‌گیری می‌کنن.
- ناشرها (صاحبان کانال/ربات) تبلیغ‌ها رو نمایش می‌دن و پورسانت می‌گیرن.
- همه چیز از طریق یک **ربات تلگرام + Mini App (Web App)** مدیریت می‌شه.
- یک **پنل ادمین** برای مدیریت، تأیید تبلیغ‌ها، پرداخت‌ها و گزارش‌ها لازمه.

جزئیات کامل محصول در `docs/PRD.md`. معماری و استک فنی در `docs/ARCHITECTURE.md`. نقشه راه فاز‌به‌فاز و چک‌لیست در `docs/ROADMAP.md`.

## ۲. اصول کار کردن (اجباری)

1. **همیشه فاز به فاز پیش برو.** فازها در `docs/ROADMAP.md` تعریف شدن. وارد فاز بعدی نشو مگر همه‌ی آیتم‌های فاز فعلی تیک بخوره و تست بشه.
2. **چک‌لیست زنده نگه دار.** فایل `docs/ROADMAP.md` رو به‌عنوان منبع حقیقت (source of truth) وضعیت پروژه به‌روزرسانی کن:
   - قبل از شروع هر تسک: آیتم رو به `[~] در حال انجام` تغییر بده.
   - بعد از اتمام و تست موفق: به `[x]` تغییر بده.
   - در **هر پاسخ به کاربر**، خلاصه‌ی وضعیت رو به‌صورت چک‌لیست تیک‌خورده (Markdown checkbox) نشون بده — نه فقط توضیح متنی. مثال:
     ```
     ✅ ساخت جدول users
     ✅ اتصال Prisma به PostgreSQL
     ⬜ نوشتن تست‌های واحد User Service
     ```
3. **قبل از تصمیمات مهم و برگشت‌ناپذیر از کاربر بپرس**، از جمله (ولی نه محدود به):
   - انتخاب/تغییر کتابخانه یا سرویس ثالث که در ARCHITECTURE.md مشخص نشده.
   - هرگونه تصمیم مربوط به پول واقعی: نرخ تبدیل سکه به ریال/تومان، درصد کمیسیون پلتفرم از ناشرها، حداقل مبلغ برداشت، اتصال به درگاه پرداخت واقعی (نیاز به merchant ID/کلید API از کاربر).
   - ساختار دیتابیس اگر نیاز به تغییر اساسی در schema تعریف‌شده باشه.
   - هر کاری که داده‌ی کاربر واقعی یا پول واقعی رو دستکاری کنه (مثلاً اجرای migration روی دیتابیس production).
4. **هیچ‌وقت credential، کلید API، توکن ربات یا رمز واقعی رو حدس نزن یا فرضی ننویس.** همیشه در `.env.example` placeholder بذار و از کاربر بخواه مقدار واقعی رو در `.env` (که در `.gitignore` هست) قرار بده.
5. **هر تسک تکمیل‌شده = یک commit جدا** با پیام واضح به فرمت Conventional Commits (مثل `feat(bot): add /start command with language selection`). هیچ‌وقت چند فاز رو در یک commit غول‌پیکر مخلوط نکن.
6. **کیفیت کد:**
   - TypeScript با `strict: true`، ممنوعیت استفاده از `any` بدون توجیه مکتوب در کامنت.
   - ESLint + Prettier از همون ابتدای پروژه فعال باشه؛ کد قبل از commit باید lint رو پاس کنه.
   - هر ماژول/سرویس مهم باید حداقل تست واحد (unit test) داشته باشه؛ منطق مالی (کیف پول، محاسبه CPM، بودجه) باید تست پوشش بالا داشته باشه (>80%).
   - کامنت‌های کد به انگلیسی، اما مستندات (docs/) و پیام‌های ربات چندزبانه (fa/en/ar) طبق i18n.
7. **امنیت (اولویت بالا چون پول واقعی درگیره):**
   - اعتبارسنجی `initData` تلگرام Mini App با HMAC-SHA256 طبق مستندات رسمی تلگرام، هرگز اعتماد کور به داده‌ی کلاینت نکن.
   - Rate limiting روی همه‌ی API های عمومی و به‌خصوص endpoint های wallet/payment.
   - هیچ مبلغی مستقیماً از ورودی کاربر بدون اعتبارسنجی سمت سرور کم/اضافه نشه (منطق مالی همیشه سمت سرور، هرگز سمت کلاینت).
   - تراکنش‌های مالی باید idempotent باشن (جلوگیری از double-spending با یک درخواست تکراری).
   - لاگ‌گذاری کامل (audit log) برای هر تراکنش کیف پول، تأیید/رد تبلیغ، و اقدام ادمین.
8. **اگر با مشکلی مواجه شدی که در مستندات پیش‌بینی نشده** (مثلاً محدودیت غیرمنتظره در Telegram Bot API)، متوقف شو، مشکل رو به‌وضوح توضیح بده، ۲-۳ راه‌حل پیشنهادی با مزایا/معایب بده و منتظر تصمیم کاربر بمون؛ خودسرانه یک راه‌حل حدسی رو پیاده نکن.
9. **مستندسازی مستمر:** هر بار که یک تصمیم فنی جدید گرفته می‌شه (مثلاً انتخاب کتابخانه‌ی خاص)، در `docs/ARCHITECTURE.md` یا یک فایل `docs/DECISIONS.md` (Architecture Decision Record) ثبتش کن.
10. **زبان گزارش‌دهی به کاربر: فارسی.** کامنت کد و نام متغیرها: انگلیسی.

## ۳. ساختار مانوریپو (Monorepo)

```
telegram-ads-platform/
├── apps/
│   ├── api/          # NestJS backend (REST + WebSocket برای آمار زنده)
│   ├── bot/          # ربات تلگرام (grammY)
│   ├── miniapp/      # React + Vite - Telegram Mini App (پنل تبلیغ‌دهنده/ناشر)
│   └── admin/        # React - پنل ادمین
├── packages/
│   ├── database/     # Prisma schema + client مشترک
│   ├── shared-types/ # DTO / type های مشترک بین api/bot/miniapp
│   └── config/       # تنظیمات مشترک (env schema با zod)
├── docs/
├── .github/workflows/
├── docker-compose.yml
├── docker-compose.prod.yml
└── CLAUDE.md
```

## ۴. تعریف "انجام شده" (Definition of Done) برای هر تسک

یک تسک وقتی تیک می‌خوره که:
- [ ] کد نوشته شده و lint/type-check پاس شده
- [ ] تست واحد مربوطه نوشته و پاس شده
- [ ] اگر API endpoint هست، در `docs/ARCHITECTURE.md` مستند شده
- [ ] commit جداگانه با پیام واضح ثبت شده
- [ ] در چک‌لیست `docs/ROADMAP.md` علامت زده شده

## ۵. شروع کار

اولین تسک تو **همیشه** خوندن `docs/PRD.md`، `docs/ARCHITECTURE.md` و `docs/ROADMAP.md` هست و سپس شروع فاز ۰ از ROADMAP. اگر سوالی قبل از شروع داری، الان بپرس نه وسط فاز ۳.
