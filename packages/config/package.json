{
  "name": "@telegram-ads/config",
  "private": true,
  "version": "0.0.1",
  "description": "Shared env loading and validation via zod (see docs/ARCHITECTURE.md #2)",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "eslint \"src/**/*.ts\" -c ../../eslint.config.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "echo \"[config] no build step - consumed as TS source via workspace\""
  },
  "dependencies": {
    "zod": "^3.23.8"
  }
}
