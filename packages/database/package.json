{
  "name": "@telegram-ads/database",
  "private": true,
  "version": "0.0.1",
  "description": "Shared Prisma schema + generated client (see docs/ARCHITECTURE.md #3)",
  "scripts": {
    "lint": "echo \"[database] no schema yet (phase 1)\"",
    "typecheck": "echo \"[database] no schema yet (phase 1)\"",
    "build": "echo \"[database] no schema yet (phase 1)\""
  }
}
